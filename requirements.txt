# 高性能 Kyash 自動チャージ Discord Bot / 依存パッケージ
# Python 3.11+ が必要です。

# Discord Bot 本体 (Slash Command / Persistent View / Modal)
discord.py>=2.3.0,<3.0.0

# 添付 Kyash モジュール (vendor/Kyasher) の依存
requests>=2.31.0
beautifulsoup4>=4.12.0

# 受取用アカウントのアクセストークンを暗号化して保存するため
cryptography>=41.0.0

# vendor/Kyasher の README が必須として挙げている依存。
# 同梱コード (Kyasher 1.5.0) では未使用だが、上流指定に合わせて導入する。
pkce>=1.0.3
